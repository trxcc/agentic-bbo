# 环境配置

每个 task package 都必须至少提供以下两种方式中的一种：

- 任务本地的 Docker 配置，例如 `Dockerfile`、`docker-compose.yml` 或 `docker/` 目录
- 明确的手动环境配置说明，使协作者能够复现任务运行环境

如果采用手动配置，建议这个文件至少写清楚：

- 所需 Python 版本或系统运行时
- 使用的包管理器与安装命令
- 外部系统依赖
- 是否依赖 GPU 或其他加速器
- 一个最小 smoke-test 命令，用于确认环境已经可用
