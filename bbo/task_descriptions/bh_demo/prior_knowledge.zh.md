# 领域先验知识

- 这个 benchmark 运行在 descriptor space 中，而不是原始试剂身份空间里，因此几何上彼此接近的点，并不一定对应“显然相似”的化学条件。
- 特征筛选通常会同时保留反应条件变量（例如温度、浓度）和 ligand/solvent descriptor；在当前封装任务里，这两类坐标都可能重要。
- 最小化 regret 等价于最大化预测收率，但 regret 变换让这个任务方向与仓库里“以最小化为主”的约定保持一致。
- 由于特征筛选依赖随机森林 seed，固定任务 seed 是这个领域协议的一部分，而不仅仅是一个实现细节。
