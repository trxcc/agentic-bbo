# Evaluation Protocol

Report the primary objective as `loss`.
Also record auxiliary metrics such as regret with respect to the known optimum and distance to the nearest known optimum.
Use the saved plots to inspect both incumbent trace and 2D sample coverage.
