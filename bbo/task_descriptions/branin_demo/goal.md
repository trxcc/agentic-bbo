# Goal

Minimize the scalar objective `loss` over the rectangular domain `x1 in [-5, 10]` and `x2 in [0, 15]`.
Each evaluation is deterministic and counts as one trial.
A strong optimizer should quickly approach the known optimum value near `0.397887` and discover one of the three global basins.
