# Submission Interface

The task accepts two continuous parameters:

- `x1`: float in `[-5.0, 10.0]`
- `x2`: float in `[0.0, 15.0]`

Each trial should log the evaluated configuration, objective, and derived metrics into JSONL.
