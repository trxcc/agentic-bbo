# 环境配置

每个真实 benchmark task 都应明确提供以下两种方式中的一种：

- 与任务一起提交的 task-local Docker 配置
- 协作者可以直接执行的手动环境配置说明

如果采用手动配置，这个文件通常应说明：

- 运行时版本要求
- 包安装命令
- 必须可用的外部服务或数据集
- 硬件要求
- smoke-test 命令

如果提供的是 Docker 工作流，则这个文件应指出 task-local Docker 入口，并说明推荐的调用方式。
