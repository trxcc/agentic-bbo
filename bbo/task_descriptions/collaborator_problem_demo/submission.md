# Submission Interface

A collaborator-facing task package should specify:

- parameter schema with types and bounds
- expected evaluator inputs and outputs
- required metadata per trial
- artifact layout for logs, plots, and summaries
