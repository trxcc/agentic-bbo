# 领域先验知识

- 这 10 个控制量都更像非负剂量或配方比例旋钮，因此相较于反复撞击边界，先在盒子内部做探索通常更容易解释。
- `P10-MIX1` 是暴露变量中唯一一个在分发数据里明显远离 0 的量；调整它时，往往比微量添加剂的微调带来更明显的几何变化。
- benchmark 返回的是变换后的 regret，而不是原始 `Target`，但优化偏好并没有改变：最小化 regret 就等价于最大化底层预测 target。
- 除了分发数据和拟合 surrogate 所体现的信息外，这个任务不额外假设更强的机理单调性。
