# Background

JOB full-knob surrogate (196 dims). The registered HTTP wrapper evaluates through the reusable Python 3.7 sidecar image `fakerstrawberry/agentbbo-dbtune-surrogate-http-py37:v1`, which bundles the RF checkpoint.
This is the full-knob surrogate for the JOB workload and predicts query latency.
