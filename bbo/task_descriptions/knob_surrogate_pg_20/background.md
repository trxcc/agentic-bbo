# Background

PostgreSQL 20-knob surrogate. The registered HTTP wrapper evaluates through the reusable Python 3.7 sidecar image `fakerstrawberry/agentbbo-dbtune-surrogate-http-py37:v1`, which bundles the RF checkpoint.
The optimizer proposes normalized PostgreSQL knob coordinates and receives the surrogate throughput objective.
