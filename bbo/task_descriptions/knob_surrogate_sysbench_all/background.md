# Background

Sysbench full-knob surrogate (196 dims). The registered HTTP wrapper evaluates through the reusable Python 3.7 sidecar image `fakerstrawberry/agentbbo-dbtune-surrogate-http-py37:v1`, which bundles the RF checkpoint.
This task exposes the full MySQL knob vector (196 active dimensions).
