# 领域先验知识

- `qed` 是确定性的，并且被限制在 `[0, 1]` 内，因此原始 QED 的每一点提升，都会线性对应到更低的报告 loss。
- benchmark 的搜索空间是一个固定候选库，而不是分子编辑或生成策略；多样性只能来自于选择不同的压缩包条目。
- 无效字符串虽然允许出现，但会被强烈支配，因为它们会坍缩到最差可能的 QED 分数。
- 除了缓存后的 ZINC 候选池，以及 RDKit 的 `MolFromSmiles` 与 `QED.qed` 之外，这个任务不额外假设药化先验。
