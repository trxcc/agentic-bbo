# 领域先验知识

- 类别型金属身份和工艺设置之间存在强交互，因此局部数值微调是否有效，往往取决于先选中了哪组金属组合。
- 清洗后的数据中显式保留了 `None` 这一类别，用来表示缺失的第二或第三金属；也就是说，“没有该金属”在这里是一个真实建模状态，而不是被当作普通缺失值丢弃。
- benchmark 搜索空间刻意把组成比例保留为彼此独立的变量；除非未来任务版本显式增加规则，否则不要默认它们一定会归一化到和为 `100`。
- 过电位越低越好；除了拟合 oracle 的确定性输出之外，这个任务没有额外噪声模型。
