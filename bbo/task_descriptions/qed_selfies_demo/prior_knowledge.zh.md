# 领域先验知识

- SELFIES 是一种更稳健的分子字符串表示，适合作为分子生成任务的离散 token 接口。
- QED 是确定性指标，取值范围在 `[0, 1]`，所以 QED 的提升会线性映射为更低的 `qed_loss`。
- 较短的有效 SELFIES 序列已经可以表示简单分子；序列长度增加会快速扩大搜索空间。
- token 词表来自本地 ZINC 示例，但成功候选可以解码为原始归档中不存在的分子。
- 无效序列不会中断实验，而是被记为最差目标值，方便传统优化器继续运行。

