# Background

The shifted sphere task is a convex synthetic objective intended for smoke tests, unit tests, and resume-path validation.
It is deliberately simple so failures point to framework issues rather than optimizer complexity.
